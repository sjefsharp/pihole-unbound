#!/bin/bash

# Path to the Unbound executable
UNBOUND_EXEC=/usr/sbin/unbound

# Start Unbound
$UNBOUND_EXEC -d

# Check if Unbound started successfully
if [ $? -eq 0 ]; then
    echo "Unbound started successfully."
else
    echo "Failed to start Unbound."
    exit 1
fi